# AI-Alignment
A repo for a basic AI pattern recognition project to generate a 3 value vector with x, y, and rotation to bring two fiducial marks into alignment. 

The folder "TrainingData" contains 1000 images of slightly missaligned plus-in-a-box fiducial marks with an accompanying CV with the offsets to be used to compare the model against. 

